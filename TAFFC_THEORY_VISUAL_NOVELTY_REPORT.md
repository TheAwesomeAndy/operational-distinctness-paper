# TAFFC Theory / Visual / Novelty Scaffold — Validation Report

Scope: the dynamical-system theory framing, explicit novelty paragraph, and two
conceptual figures added to the TAFFC manuscript variants (PR #28), plus the
redesign of the distinctness-map figure to be visually distinct from Fig. 1.

## Branch and commits

- **Branch:** `submission/taffc-theory-visual-novelty`
- **Base branch:** `claude/ieee-manuscript-overview-wjiis7` (PR #27)
- **Base commit:** `168a7e18b9979265e0499a57cc7aa700d31d8ff9`
- **Change head commit (theory + figures + redesign):** `d74f38a8e65c0ce37002dbe7b71b16689b810161`
- This report is added in a follow-up commit on the same branch; the branch tip
  after that commit is the PR #28 head.
- Commits on the branch:
  - `877db53` — theory subsection, novelty paragraph, two conceptual figures, page compression
  - `d74f38a` — redesign distinctness-map figure to be visually distinct from Fig. 1

## Files changed (vs. base `168a7e1`)

- `manuscript/taffc/main_taffc.tex`, `manuscript/taffc/main_taffc_blind.tex` — theory subsection, novelty paragraph, two figure floats; compressed redundant Discussion/Conclusion prose
- `manuscript/taffc/main_taffc.pdf`, `manuscript/taffc/main_taffc_blind.pdf` — recompiled
- `experiments/tcds_ready9/generate_taffc_theory_figures.py` — new generator for the two conceptual figures
- `figures/taffc/fig_observation_model.pdf`, `figures/taffc/fig_operational_distinctness_map.pdf` — new figures
- `TAFFC_THEORY_VISUAL_NOVELTY_REPORT.md` — this report

## Figures added

| Figure | File | Number | Page (both PDFs) | Type |
|---|---|---|---|---|
| Observation model | `fig_observation_model.pdf` | Fig. 2 | p. 3 | **Schematic** (no data) |
| Operational-distinctness map | `fig_operational_distinctness_map.pdf` | Fig. 3 | p. 4 | **Schematic** (no data) |

Both are conceptual/theoretical schematics drawn with matplotlib patches; neither
plots cohort data. (The early overview Fig. 1 from PR #27 remains a mixed
schematic + diagnostic-data figure; it is unchanged here.)

Figure-number assignment confirmed from `main_taffc.aux`:
`fig:overview`→1 (p2), `fig:obsmodel`→2 (p3), `fig:distinctmap`→3 (p4).

### Visual-distinctness check (three early figures play distinct roles)
- **Fig. 1** (overview): whole-paper pipeline (ERP → reservoir/BSC$_6$ → E,D,T,C → evaluations).
- **Fig. 2** (observation model): latent state $x_t$, stimulus drive $u_t$, projection $H$, process/observation noise, partial observation read by $\Phi$.
- **Fig. 3** (distinctness map): a streams × measured-behavior matrix (rows $E,D,T,C$; columns $S$, $1-\mathrm{CKA}$, $\rho$, $\kappa$, $U$) visualizing the distinctness vector, plus the endpoint/graph/reservoir/ARSPI-Net novelty band. The earlier chip-row + streams→evaluations flow (which echoed Fig. 1) was removed in commit `d74f38a`.

## Page counts and placement

| Variant | Pages | Fig. 1 | Fig. 2 | Fig. 3 |
|---|---|---|---|---|
| `main_taffc.pdf` | 12 | p. 2 | p. 3 | p. 4 |
| `main_taffc_blind.pdf` | 12 | p. 2 | p. 3 | p. 4 |

Both within the TAFFC 12-page free limit. The additions are page-neutral relative
to the post-PR-27 state because the new single-column figures and theory text are
offset by tightening redundant Discussion/Conclusion prose.

## Compile commands

Run from `manuscript/` (shared figures/tables resolve relative to that directory):

```
cd manuscript
pdflatex -interaction=nonstopmode -output-directory=taffc taffc/main_taffc.tex
(cd taffc && bibtex main_taffc)
pdflatex -interaction=nonstopmode -output-directory=taffc taffc/main_taffc.tex
pdflatex -interaction=nonstopmode -output-directory=taffc taffc/main_taffc.tex
```

Identical sequence for `main_taffc_blind.tex`. Figures regenerated with:

```
python experiments/tcds_ready9/generate_taffc_theory_figures.py
```

## Validation counts

| Metric | main_taffc | main_taffc_blind |
|---|---|---|
| Pages | 12 | 12 |
| Undefined references | 0 | 0 |
| Duplicate (multiply-defined) labels | 0 | 0 |
| Duplicate bibitems (BibTeX "repeated entry") | 0 | 0 |
| Missing citations | 0 | 0 |

## Privacy scan

- The new generator (`generate_taffc_theory_figures.py`) reads **no data files**; the
  two figures are purely schematic.
- String/metadata scan of the new script, both new figure PDFs, and both recompiled
  manuscript PDFs: **0** occurrences of absolute paths (`/home/`, `/Users/`), raw
  subject IDs (`SHAPE_Community`), or PHI/clinical fields. Figure PDF Producer is
  `Matplotlib`; manuscript PDFs are `pdfTeX`.

## AI / vendor / session-string scan

- **0** occurrences of `claude`, `anthropic`, or `session_01…` in the new script, the
  figure PDFs, the manuscript PDFs, or the commit messages.
- The auto-appended "Generated by Claude Code" footer was removed from the PR #28 body.

## No-change confirmations

- **No numerical result changed** — no results code, output, or reported value was touched.
- **No CSV changed** — no file under `outputs/`, `data_public/`, or `data/` was modified.
- **No subject or observation count changed** — 211 subjects / 633 subject-condition observations unchanged.
- **No table changed** — no `tables/**` file modified.
- **No shared TCDS file or shared TCDS figure changed** — `manuscript/main.tex`, `manuscript/main_blind.tex`, `tables/tcds_ready9/*`, `manuscript/figures/architecture/*`, `manuscript/figures/tcds_hardening/*`, and `manuscript/figures/tcds_ready9/*` are untouched.
- **All citations retained** — text compression removed only duplicated prose, not any `\cite`; the reference list is unchanged.
- **All required scope admissions retained** — conventional ERP-amplitude baseline is stronger; no classifier-superiority claim; no diagnostic validation; no online/real-time BCI; trial-averaged ERP scope; closed-loop simulation scope; access-controlled / aggregate-only data statement.

## Integration order (post-PR-27)

PR #28 is currently **stacked** on `claude/ieee-manuscript-overview-wjiis7` and is **not**
merged. Required final-integration order:

1. Finalize and **merge PR #27** into `main`.
2. **Rebase / update PR #28 onto the new `main`** (do not rely on the stacked-branch compile).
3. **Recompile** both TAFFC variants from `manuscript/`.
4. **Reverify** page counts (12/12), figure numbering (1/2/3 for overview/observation/distinctness), undefined references (0), duplicate labels (0), citations, privacy, and the no-numerical-change confirmations above.
5. Then review PR #28 for merge.

## Residual risks

- **Public data governance.** The repository is public and tracks raw per-subject EEG
  (`data/batch_data_full/*.txt`, subject IDs in filenames), per-subject clinical labels
  (`data/clinical_profile.csv`), and the feature pickle. The repository owner has stated
  this is intended. This rests on **owner assertion**; it is not the same as documented
  IRB / data-use-agreement / collaborator sign-off. **Recommendation:** retain documented
  authorization for public release before journal submission, or remediate (untrack +
  gitignore + history purge, and/or make the repository private). This is independent of
  PR #28 (the data files are not part of this PR).
- **Local-build PDFs.** The committed PDFs were produced by the local TeX Live install;
  byte output may differ from the author's environment, though content and validation
  results are identical.
- **Stacked-branch numbering.** Figure numbers (1/2/3) were verified on the stacked
  branch; they must be re-verified after rebasing onto a post-PR-27 `main` (step 4 above).
